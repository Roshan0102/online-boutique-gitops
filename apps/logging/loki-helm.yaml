apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki
  namespace: argocd

spec:
  project: default

  destination:
    server: https://kubernetes.default.svc
    namespace: logging

  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: loki
    targetRevision: 6.38.0

    helm:
      valueFiles:
        - $values/apps/logging/loki-values.yaml

  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: loki
      targetRevision: 6.38.0
      helm:
        valueFiles:
          - $values/apps/logging/loki-values.yaml

    - repoURL: https://github.com/roshan0102/online-boutique-gitops.git
      targetRevision: main
      ref: values

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
